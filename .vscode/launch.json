{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Sentinel",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Sentinel.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "preLaunchTask": "Build Sentinel"
        }
    ]
}
